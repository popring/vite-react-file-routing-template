# Vite React File Routing Starter

A modern React + TypeScript starter template for building animated web applications, featuring:

- ⚡️ **Vite + Rolldown** - Lightning-fast development and build
- ⚛️ **React 19** with React Compiler for automatic optimizations
- 🗂️ **File-based routing** with vite-plugin-pages
- 🎨 **Tailwind CSS v4** with @tailwindcss/vite
- 📝 **TypeScript** for type safety
- 🔍 **ESLint** configured for React 19

## Getting Started

```bash
# Install dependencies
pnpm install

# Start dev server
pnpm dev

# Build for production
pnpm build
```

## Project Structure

```
src/
  └── pages/          # Auto-routed pages
      ├── index.tsx   # Home page (/)
      ├── page-a/     # /page-a route
      └── page-b.tsx  # /page-b route
```

Perfect for building animated web applications with a solid foundation. 🚀

